"use client";
import { useEffect, useState } from "react";
import { api } from "../../lib/api";
export default function StoreLedgerPage(){const [rows,setRows]=useState<any[]>([]);const [err,setErr]=useState("");async function load(){try{setRows(await api<any[]>("/store/ledger"));setErr("");}catch(e){setErr(e instanceof Error?e.message:"โหลดไม่ได้")}}useEffect(()=>{void load()},[]);const total=rows.reduce((s,r)=>s+Number(r.amount||0),0);return <main className="app-shell"><section className="hero"><div className="kicker">Finance</div><h1>Store Ledger</h1><p className="muted">บัญชีเงินเข้าร้านจาก payment request, adjustment, refund และ platform fee</p><div className="hero-actions"><button className="btn secondary" onClick={load}>รีเฟรช</button><a className="btn secondary" href="/payment-requests">Payment Requests</a></div></section>{err&&<div className="alert bad">{err}</div>}<div className="grid cols-3"><section className="card metric"><div className="metric-label">ยอดรวม</div><div className="metric-value">฿{total.toLocaleString()}</div><div className="metric-note">จากรายการที่แสดง</div></section><section className="card metric"><div className="metric-label">รายการ</div><div className="metric-value">{rows.length}</div><div className="metric-note">ล่าสุด 200 รายการ</div></section><section className="card metric good"><div className="metric-label">พร้อมบัญชี</div><div className="metric-value">CSV</div><div className="metric-note">ใช้ export payment report เพิ่มเติม</div></section></div><section className="card" style={{marginTop:16}}><div className="table-wrap"><table className="table"><thead><tr><th>เวลา</th><th>ประเภท</th><th>ยอด</th><th>สถานะ</th><th>อ้างอิง</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{new Date(r.createdAt).toLocaleString()}</td><td>{r.type}</td><td>฿{Number(r.amount).toLocaleString()}</td><td><span className="badge good">{r.status}</span></td><td>{r.reference}<div className="small">{r.note}</div></td></tr>)}</tbody></table></div></section></main>}
