"use client";
import { useEffect, useState } from "react";
import { api } from "@/lib/api";
export default function PlatformMdmDevicesPage(){const [rows,setRows]=useState<any[]>([]);const [err,setErr]=useState("");async function load(){try{setRows(await api<any[]>("/platform/mdm/devices"));setErr("");}catch(e){setErr(e instanceof Error?e.message:"โหลดไม่ได้")}}useEffect(()=>{void load()},[]);return <main className="app-shell"><section className="hero"><div className="kicker">Platform MDM</div><h1>All Managed Devices</h1><p className="muted">เครื่อง Android/iOS/iCloud custody รวมทุก tenant แต่แสดงเฉพาะ Platform Owner</p><div className="hero-actions"><button className="btn secondary" onClick={load}>รีเฟรช</button><a className="btn secondary" href="/platform/mdm/commands">ดูคำสั่ง</a></div></section>{err&&<div className="alert bad">{err}</div>}<section className="card"><div className="table-wrap"><table className="table"><thead><tr><th>ร้าน</th><th>เครื่อง</th><th>Platform</th><th>Control</th><th>Status</th><th>Provider</th></tr></thead><tbody>{rows.map(d=><tr key={d.id}><td>{d.organization?.name}</td><td>{d.brand} {d.model}<div className="small">SN: {d.serialNumber||"-"} IMEI: {d.imei||"-"}</div></td><td>{d.platform}</td><td>{d.controlMode}</td><td><span className="badge">{d.controlStatus}</span></td><td>{d.providerDeviceName||d.providerEnrollmentId||"-"}</td></tr>)}</tbody></table></div></section></main>}
