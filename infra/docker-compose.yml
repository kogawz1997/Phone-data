version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: phone_finance_postgres
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: phone_finance
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: phone_finance_redis
    ports:
      - "6379:6379"

volumes:
  postgres_data:
